# Server Configuration
PORT=5000
NODE_ENV=development
JWT_SECRET=super_secret_hostel_jwt_key_2026

# PostgreSQL Database Configuration
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=postgres
PGDATABASE=hostel_db

# Or direct Connection String URI:
# DATABASE_URL=postgres://postgres:postgres@localhost:5432/hostel_db
